{
  "name": "luma-ingestion-extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.36",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
